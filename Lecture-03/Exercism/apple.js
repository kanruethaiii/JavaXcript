function chack(values1, values2, values3) {
    var return ;
    for(var i = 0; i< values1.length; i++) {
        if(values1[0] === values2[i]) {
            return = values2=[i];
        }
        else if()
    }
}
let values1 = ['Apple', 1, false];
let values2 = ['Fries', 2, true];
let values3 = ['Mars', 9, 'Apple'];

